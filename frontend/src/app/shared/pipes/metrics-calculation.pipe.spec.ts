import { MetricsCalculationPipe } from './metrics-calculation.pipe';

describe('MetricsCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricsCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
